// function symmetry(node) {
//   if (!node.left && node.right) return false;
//   if (node.left && !node.right) return false;
//   if (!node.left && !node.right) return right;

//   let left = node.left;
//   let right = node.right;
//   return left.value === right.value && symmetry(left) && symmetry(right);
// }
